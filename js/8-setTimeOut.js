// SETTIMEOUT JS MODERNO

/*
    Estrutura básica:

setTimeout(() => {
    função a ser executada
}, timeout(após quanto tempo deve ser executada));

*/

setTimeout(()=>{
    console.log(`Após dois segundos.`)
},2000)

// O timeout é passado em milisegundos, ou seja, 2000 ms = 2 s.

clearInterval(id) 

// Clear = cancela a execução
